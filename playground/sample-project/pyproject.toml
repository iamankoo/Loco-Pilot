[project]
name = "calculator-fixture"
version = "0.1.0"
description = "Minimal deterministic fixture project used to prove LocoPilot's real, sandboxed test execution end to end."
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0",
]
